import { Container } from "@mui/material";
import { HeroSection, HeroTitle } from "./HeroImg.styled"

export const HeroImg = () => {
  return (
    <HeroSection>
      <Container>
        <HeroTitle>
          Phone Book
        </HeroTitle>
      </Container>
    </HeroSection>
  );
};
